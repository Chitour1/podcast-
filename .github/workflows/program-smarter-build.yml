name: Program Smarter Android Standalone Build

on:
  workflow_dispatch:

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build temporarily paused while the full Program Smarter source is committed."
