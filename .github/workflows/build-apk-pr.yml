name: Verify PodcastForge APK
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Restore Studio 5 base and overlay Studio 6 engine
        shell: bash
        run: |
          cat studio5.part00.b64 studio5.part01.b64 studio5.part02.b64 studio5.part03.b64 | base64 -d > base.zip
          unzip -q base.zip
          cp -rf studio6src/app/* PodcastForge/app/
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK 36
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Build Studio 6 debug APK
        working-directory: PodcastForge
        run: gradle --no-daemon assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: PodcastForge-Studio-6-APK
          path: PodcastForge/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
