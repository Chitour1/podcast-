# View Source Ultra
Android app that takes a URL and fetches the original HTTP response source (View Source), follows redirects, detects common character encodings, displays the complete source, and supports copy/save.
