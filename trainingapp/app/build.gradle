plugins { id 'com.android.application' }

android {
    namespace 'com.acrps.voxaridetraining'
    compileSdk 35

    defaultConfig {
        applicationId 'com.acrps.voxaridetraining'
        minSdk 26
        targetSdk 35
        versionCode 1
        versionName '1.0.0'
    }

    buildTypes {
        debug { minifyEnabled false }
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
